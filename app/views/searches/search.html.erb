<h1>Search for Coffee Shops Near Me</h1>
<%= form_tag '/search' do %>
  <%= label_tag :zipcode %>
  <%= text_field_tag :zipcode %>
  <%= submit_tag "Search!" %>
<% end %>

<div>
  <!-- NOTE : Checking response : < %= @resp.body if @resp %>-->
  <!-- NOTE : Checking new ruby hash : < %= @venues %>-->

  <!-- NOTE : Below = only pulling desired info from hash -->
  <!--< % if @venues %>-->
  <!--  <ul>-->
  <!--  < % @venues.each do |venue| %>-->
  <!--    <li>-->
  <!--      < %= venue["name"] %><br>-->
  <!--      < %= venue["location"]["address"] %><br>-->
  <!--      Checkins: < %= venue["stats"]["checkinsCount"] %>-->
  <!--    </li>-->
  <!--  < % end %>-->
  <!--  </ul>-->
  <!--< % end %>-->

  <!-- NOTE : Using error handling -->
  <% if @error %>
    <p><%= @error %></p>
  <% elsif @venues %>
    <ul>
    <% @venues.each do |venue| %>
      <li>
        <%= venue["name"] %><br>
        <%= venue["location"]["address"] %><br>
        Checkins: <%= venue["stats"]["checkinsCount"] %>
      </li>
    <% end %>
    </ul>
  <% end %>


</div>
